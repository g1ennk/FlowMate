# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=flowmate_reports
DATABASE_USER=flowmate
DATABASE_PASSWORD=flowmate

# JWT (same as Spring backend — hex-encoded HS256 secret)
JWT_SECRET=<same-hex-secret-as-spring>

# Gemini AI
GEMINI_API_KEY=your-gemini-api-key

# Spring API
FLOWMATE_API_URL=http://localhost:8080/api

# Server
PORT=3000
